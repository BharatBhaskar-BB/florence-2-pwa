# Florence-2 BundleBox PWA — Python dependencies
# Note: torch + torchvision come from the Docker base image (pytorch:2.4.0-cuda12.4)
# Do NOT upgrade them — the GCP driver only supports up to CUDA 12.8
torch==2.4.0
torchvision==0.19.0
transformers==4.46.3
einops==0.8.2
timm==1.0.26
fastapi==0.135.3
uvicorn==0.44.0
pillow>=10.0.0
google-genai>=1.0.0
pydantic>=2.0.0
python-multipart>=0.0.6
# Segmentation models
opencv-python-headless>=4.8.0
huggingface-hub>=0.20.0
# MobileSAM installed from git in Dockerfile
# SAM 2 installed via pip in Dockerfile
